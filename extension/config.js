// CampusShield AI Extension Global Configuration
const CONFIG = {
    // When deploying the backend to Render, change this URL to the live HTTPS URL.
    // Example: "https://campusshield-api.onrender.com/api"
    API_BASE: "http://localhost:8000/api"
};
